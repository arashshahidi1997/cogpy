You are inspecting how CogPy represents spectral, spatial, and feature data.

INPUT DOCUMENT

docs/log/dev/feature-review/feature-review-01-inventory.md

Also inspect the conceptual registry:

docs/reference/feature-registry

Your goal is to extract the **actual xarray schemas used in CogPy** so future feature implementations can remain compatible.

Focus on transforms such as:

psdx
spectrogram-like transforms
channel feature maps
AP×ML grid transforms
windowed transforms

Determine:

1) dimensions used
2) coordinates attached
3) attrs conventions
4) return types
5) validators / dimension constants
6) compatibility with pipelines

Inspect code paths such as:

src/cogpy/core/spectral
src/cogpy/core/measures
src/cogpy/core/utils
src/cogpy/core/preprocess

OUTPUT FILE

docs/log/dev/feature-review/feature-review-02-schema.md

STRUCTURE

# CogPy Data Schema Review

## Spectral outputs
dims:
coords:
attrs:
return type:

## Spectrogram outputs
dims:
coords:
attrs:

## Channel feature maps
dims:
coords:
attrs:

## Grid outputs (AP × ML)
dims:
coords:
attrs:

## Event outputs
table schema
fields

## Dimension constants
List dimension names used across the codebase.

## Validators
Describe existing validation helpers.

## psdx compatibility
Describe how psdx outputs compose with downstream transforms.

## Observed conventions
List recurring schema conventions.

## Potential friction points
Note areas where schema consistency is weak.