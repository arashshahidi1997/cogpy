You are working inside the CogPy repository.

Your task is to perform an inventory of existing implementations relevant to signal-quality and noise-characterization features.

IMPORTANT CONTEXT

A conceptual feature registry exists here:

docs/reference/feature-registry

This registry describes the conceptual features expected in the system. Use it as a guide to search for implementations or partial implementations in the codebase.

You should identify where these concepts already exist in CogPy.

DO NOT propose new architecture yet. Only produce an inventory.

Primary search locations include:

src/cogpy/core/spectral
src/cogpy/core/measures
src/cogpy/core/detect
src/cogpy/core/preprocess
src/cogpy/core/utils

Also inspect related modules if needed.

Your goal is to find implementations related to:

Spectral features
- narrowband ratio
- narrowband-to-broadband ratio
- spectral peak detection
- spectral flatness
- F-test line detection (mtm_fstat)
- AM artifact detection
- line noise detection

Spatial features
- Moran's I
- spatial coherence
- spatial gradients
- spatial FFT
- row/column energy measures
- spatial kurtosis
- spatial autocorrelation
- grid-based measures

Normalization / preprocessing
- z-score across channels
- robust z-score
- neighborhood normalization
- channel normalization

Detection / events
- burst detection
- event detectors
- pipeline-based detection

For each relevant symbol record:

status:
EXISTS
PARTIAL
RELATED

Also record:

module path
symbol name
type (function/class/transform/helper)
short description
notes

OUTPUT FORMAT

Write the results to the following file:

docs/log/dev/feature-review/feature-review-01-inventory.md

Structure the document as:

# CogPy Feature Inventory

## Context
Explain briefly that the scan was guided by the conceptual registry at:
docs/reference/feature-registry

## Spectral features
(table)

## Spatial features
(table)

## Normalization features
(table)

## Detection / pipeline features
(table)

## Observations
Short bullet list of notable patterns or gaps.

IMPORTANT

Do not speculate about design or implementation yet. Only document what exists.