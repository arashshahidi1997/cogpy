You are analyzing how CogPy components are composed in practice.

IMPORTANT CONTEXT

CogPy may not contain a mature internal pipeline framework.
Real orchestration may instead happen in an external project pipeline.

Use the following as an example orchestration reference:

docs/reference/example-snakemake-pipeline

This is expected to be a symlink to a representative PixECoG pipeline, containing items such as:
- Snakefile
- scripts/
- config.yml
- notebooks/
- report/
- _docs/
- Makefile

The Snakefile calls scripts from the scripts folder.

Your goal is NOT to assess Snakemake itself.
Your goal is to understand:
1. which CogPy transforms/measures/utilities are actually composed in real workflows
2. what interface assumptions those workflows make about CogPy
3. what minimal composition abstractions, if any, would be useful inside CogPy

INPUT DOCUMENTS

docs/log/dev/feature-review/feature-review-01-inventory.md
docs/log/dev/feature-review/feature-review-02-schema.md

Also consult:
docs/reference/feature-registry
docs/reference/example-snakemake-pipeline

Inspect both:
- CogPy library code
- the example Snakemake pipeline and called scripts

Focus on these questions:

1. Which CogPy functions/classes are used directly in scripts?
2. What kinds of inputs/outputs are expected by those scripts?
3. Are CogPy outputs already schema-stable enough for scripted composition?
4. Where are event-like objects, feature tables, PSD/spectrogram outputs, or grid maps passed between steps?
5. What repeated composition patterns exist in the example pipeline?
6. Are there outdated or abandoned internal pipeline abstractions in CogPy that should not be treated as source-of-truth?
7. What lightweight reusable composition helpers inside CogPy would reduce duplication in external pipelines?

OUTPUT FILE

docs/log/dev/feature-review/feature-review-03-composition.md

STRUCTURE

# CogPy Composition and Orchestration Review

## Context
Explain that this review treats external project orchestration as the primary source of practical composition patterns.

## CogPy components used in practice
List functions/classes/modules used by the example pipeline scripts.

## Data flow patterns
Describe how data moves between steps:
- file-based outputs
- xarray objects
- tables
- event-like outputs
- intermediate features

## Script-level composition patterns
Identify recurring patterns in the example scripts.

## Outdated internal pipeline abstractions
List any internal abstractions in CogPy that appear outdated, partial, or not representative of current practice.

## Interface assumptions imposed on CogPy
Describe what the external pipeline expects from CogPy:
- input schemas
- output schemas
- naming
- serialization expectations
- xarray compatibility

## Lightweight composition opportunities
Identify small reusable abstractions that would help external pipelines without turning CogPy into a workflow engine.

## Observations
Summarize the main implications for feature implementation.