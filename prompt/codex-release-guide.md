# Release Notes Prompt Template

<!-- TODO: Document which release checklist items apply to this repo. -->

## Prompt File

- Location: `prompt/release.prompt.md`
- Fill in each placeholder bullet with the actual entry text before use.
- Shared configuration lives in `prompt/prompt-paths.yaml` (`python`, `conda`, `conda_env`).

Once complete, copy the prompt text into Codex so it can update the release notes consistently.

<!-- TODO: Specify where the release notes live (file path, changelog section, etc.). -->
