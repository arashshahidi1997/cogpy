Use the shared path variables in `prompt/prompt-paths.yaml` (`python`, `conda`, `conda_env`, `test_env`).

Codex, please run the project test suite:

1. <insert setup command>
2. <insert primary test command>
3. <insert any follow-up verification or artifact collection steps>
