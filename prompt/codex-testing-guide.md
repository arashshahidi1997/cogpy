# Testing Prompt Template

<!-- TODO: Describe the testing scope and any caveats for running locally vs. CI. -->

## Prompt File

- Location: `prompt/testing.prompt.md`
- Replace each numbered placeholder with the concrete commands or scripts.
- Shared configuration lives in `prompt/prompt-paths.yaml` (`python`, `conda`, `conda_env`, `test_env`).

Send the prompt file contents to Codex after editing so it can execute the testing workflow verbatim.

<!-- TODO: Provide concrete commands for steps (1)-(3). -->
