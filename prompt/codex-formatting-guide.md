# Formatting & Lint Prompt Template

<!-- TODO: Clarify which linters/formatters are required and any ordering constraints. -->

## Prompt File

- Location: `prompt/formatting.prompt.md`
- Replace the numbered instructions with the exact formatter/linter commands.
- Shared configuration lives in `prompt/prompt-paths.yaml` (`python`, `conda`, `conda_env`).

Once the placeholders are updated, paste the prompt file content into Codex so it can run the commands sequentially.

<!-- TODO: Replace the placeholder steps with real commands (e.g., black, ruff, mypy). -->
